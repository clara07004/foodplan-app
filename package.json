{
  "name": "foodplan",
  "version": "1.0.0",
  "description": "FoodPlan — Planejador Semanal de Refeições",
  "main": "index.js",
  "scripts": {
    "build": "echo 'sem build necessário'",
    "cap": "npx cap"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
